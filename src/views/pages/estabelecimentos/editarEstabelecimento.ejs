<!DOCTYPE html>
<html lang="pt-br">
<%- include('../../partials/head', { title: 'Editar Estabelecimento' }) %>

<body class="form-page-body">
    <%- include('../../partials/header') %>
    <div class="page-spacer"></div>

    <% const enabledProductOptions = Array.isArray(productOptions) ? productOptions : []; %>
    <% const singleProductMode = enabledProductOptions.length === 1; %>
    <% const singleProduct = singleProductMode ? enabledProductOptions[0] : null; %>

    <div class="form-shell form-shell-modern estabelecimentos-form-shell">
        <%- include('../../partials/breadcrumb', { pageTitle: 'Editar Estabelecimento' }) %>

        <section class="page-titlebar page-titlebar-neutral">
            <div class="page-titlebar-content">
                <span class="form-kicker"><i class="bi bi-pencil-square"></i> Administrativo</span>
                <h1>Editar estabelecimento</h1>
                <p>Atualize os dados do ponto e, se necessário, salve novamente a localização usada nas rotas.</p>
            </div>

            <div class="page-titlebar-side">
                <div class="page-titlebar-stat">
                    <strong>Registro atual</strong>
                    <span><%= estabelecimento.estabelecimento %></span>
                </div>

                <div class="page-titlebar-stat">
                    <strong>Ação</strong>
                    <span>Revisar e atualizar dados do ponto</span>
                </div>
            </div>
        </section>

        <main class="form-stage">
            <form id="edicaoForm" class="modern-form estabelecimento-form" action="/estabelecimentos/<%= estabelecimento.id %>?_method=PUT" method="POST">

                <section class="form-card">
                    <div class="form-card-header">
                        <div>
                            <span class="form-section-tag">Operação</span>
                            <h2><%= singleProductMode ? 'Produto ativo e observações' : 'Produtos e observações' %></h2>
                            <p>
                                <%= singleProductMode
                                    ? 'Confira a ferramenta disponível neste plano e registre comentários úteis para próximas visitas.'
                                    : 'Atualize os produtos ativos no ponto e registre comentários úteis para próximas visitas.'
                                %>
                            </p>
                        </div>
                    </div>

                    <div class="row g-4 align-items-start">
                        <div class="col-12 col-lg-5">
                            <label><%= singleProductMode ? 'Plano atual' : 'Produtos vinculados' %></label>

                            <% if (singleProductMode) { %>
                                <input
                                    type="hidden"
                                    name="produto"
                                    value="<%= singleProduct.value %>"
                                >

                                <div class="single-product-card <%= singleProduct.className || '' %>">
                                    <div class="single-product-card-icon">
                                        <i class="bi bi-check2-circle"></i>
                                    </div>

                                    <div class="single-product-card-content">
                                        <span>1 ferramenta ativa</span>
                                        <strong><%= singleProduct.label %></strong>
                                        <small>Este é o produto disponível no plano atual. Este ponto será mantido nessa operação.</small>
                                    </div>
                                </div>
                            <% } else { %>
                              <div class="estabelecimento-products-grid">
                                <% enabledProductOptions.forEach((produto, index) => { %>
                                    <label class="estabelecimento-product-option <%= produto.className %>" for="produto<%= index + 1 %>">
                                        <input
                                            class="form-check-input"
                                            type="checkbox"
                                            id="produto<%= index + 1 %>"
                                            name="produto"
                                            value="<%= produto.value %>"
                                            <%= hasProduto(estabelecimento.produto, produto.value) ? 'checked' : '' %>
                                        >
                                        <span><%= produto.label %></span>
                                    </label>
                                <% }) %>
                            </div>

                                <small class="modern-note">Deixe pelo menos um produto marcado para manter os atalhos e relatórios coerentes com o ponto.</small>
                            <% } %>
                        </div>

                        <div class="col-12 col-lg-7">
                            <label for="observacoes">Comentários</label>
                            <textarea
                                class="form-control"
                                id="observacoes"
                                name="observacoes"
                                rows="5"
                                maxlength="255"
                            ><%= estabelecimento.observacoes || '' %></textarea>
                        </div>
                    </div>
                </section>

                <section class="form-card">
                    <div class="form-card-header">
                        <div>
                            <span class="form-section-tag">Identificação</span>
                            <h2>Dados principais do estabelecimento</h2>
                            <p>Ajuste o nome do ponto, o responsável e o telefone de contato para manter a base atualizada.</p>
                        </div>
                    </div>

                    <div class="row g-3">
                        <div class="col-12 col-lg-5">
                            <label for="estabelecimento">Estabelecimento</label>
                            <input
                                type="text"
                                class="form-control"
                                id="estabelecimento"
                                name="estabelecimento"
                                value="<%= estabelecimento.estabelecimento || '' %>"
                                required
                            >
                        </div>

                        <div class="col-12 col-md-6 col-lg-3 centered-field">
                            <label for="responsavel_nome">Responsável</label>
                            <input
                                type="text"
                                class="form-control"
                                id="responsavel_nome"
                                name="responsavel_nome"
                                value="<%= estabelecimento.responsavel_nome || '' %>"
                                required
                            >
                        </div>

                        <div class="col-12 col-md-6 col-lg-4 centered-field">
                            <label for="telefone_contato">Contato</label>
                            <input
                                type="tel"
                                class="form-control"
                                id="telefone_contato"
                                name="telefone_contato"
                                placeholder="(00) 00000-0000"
                                value="<%= estabelecimento.telefone_contato || '' %>"
                                required
                            >
                        </div>
                    </div>
                </section>

                <section class="form-card">
                    <div class="form-card-header">
                        <div>
                            <span class="form-section-tag">Localização</span>
                            <h2>Endereço e localização do ponto</h2>
                            <p>Revise endereço e bairro. Se estiver no local, atualize a localização usada nas rotas.</p>
                        </div>
                    </div>

                    <div class="row g-3">
                        <div class="col-12 col-lg-8">
                            <label for="endereco">Endereço</label>
                            <input
                                type="text"
                                class="form-control"
                                id="endereco"
                                name="endereco"
                                value="<%= estabelecimento.endereco || '' %>"
                                required
                            >
                        </div>

                        <div class="col-12 col-lg-4">
                            <label for="bairro">Bairro</label>
                            <input
                                type="text"
                                class="form-control"
                                id="bairro"
                                name="bairro"
                                value="<%= estabelecimento.bairro || '' %>"
                                required
                            >
                        </div>

                        <div class="col-12 d-none" data-operational-field="bolinhas-machine">
                            <div class="product-setup-block estabelecimento-operational-block theme-green">
                                <div class="product-setup-header">
                                    <div class="product-setup-badges">
                                        <span class="product-badge product-badge-green">
                                            <i class="bi bi-circle-fill"></i>
                                            Bolinhas
                                        </span>
                                    </div>

                                    <h3>Máquina de bolinhas</h3>
                                    <p>Campos opcionais para registrar a chave e o número da máquina de bolinhas instalada neste ponto.</p>
                                </div>

                                <div class="product-divider"></div>

                                <div class="row g-3">
                                    <div class="col-12 col-md-6 compact-field estabelecimento-operational-field">
                                        <label for="chave_bolinhas">
                                            Chave da máquina de bolinhas <span class="optional-label">opcional</span>
                                        </label>
                                        <input
                                            type="text"
                                            class="form-control estabelecimento-operational-input"
                                            id="chave_bolinhas"
                                            name="chave_bolinhas"
                                            placeholder="Código da chave"
                                            value="<%= estabelecimento.chave_bolinhas || estabelecimento.chave || '' %>"
                                        >
                                    </div>

                                    <div class="col-12 col-md-6 compact-field estabelecimento-operational-field">
                                        <label for="maquina_bolinhas">
                                            Número da máquina de bolinhas <span class="optional-label">opcional</span>
                                        </label>
                                        <input
                                            type="text"
                                            class="form-control estabelecimento-operational-input"
                                            id="maquina_bolinhas"
                                            name="maquina_bolinhas"
                                            placeholder="Número da máquina"
                                            value="<%= estabelecimento.maquina_bolinhas || estabelecimento.maquina || '' %>"
                                        >
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="col-12 d-none" data-operational-field="consignados-initial">
                            <div class="product-setup-block estabelecimento-operational-block theme-blue">
                                <div class="product-setup-header">
                                    <div class="product-setup-badges">
                                        <span class="product-badge product-badge-blue">
                                            <i class="bi bi-circle-fill"></i>
                                            Consignados
                                        </span>
                                    </div>

                                    <h3>Parâmetro inicial de consignados</h3>
                                    <p>Informe a quantidade inicial deixada para manter o controle deste produto no estabelecimento.</p>
                                </div>

                                <div class="product-divider"></div>

                                <div class="row g-3">
                                    <div class="col-12 col-md-6 col-lg-4 compact-field estabelecimento-operational-field">
                                        <label for="consignado_quantidade_inicial">
                                            Quantidade inicial deixada <span class="required-label">obrigatório</span>
                                        </label>
                                        <input
                                            type="text"
                                            inputmode="numeric"
                                            pattern="[0-9]*"
                                            maxlength="6"
                                            class="form-control estabelecimento-operational-input"
                                            id="consignado_quantidade_inicial"
                                            name="consignado_quantidade_inicial"
                                            value="<%= estabelecimento.consignado_quantidade_inicial || estabelecimento.figurinha_quantidade_inicial || '' %>"
                                            placeholder="Ex.: 100"
                                        >
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="col-12 d-none" data-operational-field="pelucias-machine">
                            <div class="product-setup-block estabelecimento-operational-block theme-violet">
                                <div class="product-setup-header">
                                    <div class="product-setup-badges">
                                        <span class="product-badge product-badge-violet">
                                            <i class="bi bi-circle-fill"></i>
                                            Pelúcias
                                        </span>
                                    </div>

                                    <h3>Máquina de pelúcias</h3>
                                    <p>Campos opcionais para registrar a chave e o número da máquina de pelúcias instalada neste ponto.</p>
                                </div>

                                <div class="product-divider"></div>

                                <div class="row g-3">
                                    <div class="col-12 col-md-6 compact-field estabelecimento-operational-field">
                                        <label for="chave_pelucias">
                                            Chave da máquina de pelúcias <span class="optional-label">opcional</span>
                                        </label>
                                        <input
                                            type="text"
                                            class="form-control estabelecimento-operational-input"
                                            id="chave_pelucias"
                                            name="chave_pelucias"
                                            placeholder="Código da chave"
                                            value="<%= estabelecimento.chave_pelucias || '' %>"
                                        >
                                    </div>

                                    <div class="col-12 col-md-6 compact-field estabelecimento-operational-field">
                                        <label for="maquina_pelucias">
                                            Número da máquina de pelúcias <span class="optional-label">opcional</span>
                                        </label>
                                        <input
                                            type="text"
                                            class="form-control estabelecimento-operational-input"
                                            id="maquina_pelucias"
                                            name="maquina_pelucias"
                                            placeholder="Número da máquina"
                                            value="<%= estabelecimento.maquina_pelucias || '' %>"
                                        >
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="col-12 d-none" data-operational-field="pelucias-initial">
                            <div class="product-setup-block estabelecimento-operational-block theme-violet">
                                <div class="product-setup-header">
                                    <div class="product-setup-badges">
                                        <span class="product-badge product-badge-violet">
                                            <i class="bi bi-circle-fill"></i>
                                            Pelúcias
                                        </span>
                                    </div>

                                    <h3>Parâmetros iniciais de pelúcias</h3>
                                    <p>Esses dados são usados pelo sistema para calcular estoque, vendas e próximas sangrias.</p>
                                </div>

                                <div class="product-divider"></div>

                                <div class="row g-3">
                                    <div class="col-12 col-md-6 col-lg-3 compact-field estabelecimento-operational-field">
                                        <label for="pelucia_leitura_inicial">
                                            Leitura inicial <span class="required-label">obrigatório</span>
                                        </label>
                                        <input
                                            type="text"
                                            inputmode="numeric"
                                            pattern="[0-9]*"
                                            maxlength="6"
                                            class="form-control estabelecimento-operational-input"
                                            id="pelucia_leitura_inicial"
                                            name="pelucia_leitura_inicial"
                                            value="<%= estabelecimento.pelucia_leitura_inicial || '' %>"
                                            placeholder="Ex.: 0"
                                        >
                                    </div>

                                    <div class="col-12 col-md-6 col-lg-3 compact-field estabelecimento-operational-field">
                                        <label for="pelucia_abastecido_inicial">
                                            Qtde. Inicial <span class="required-label">obrigatório</span>
                                        </label>
                                        <input
                                            type="text"
                                            inputmode="numeric"
                                            pattern="[0-9]*"
                                            maxlength="6"
                                            class="form-control estabelecimento-operational-input"
                                            id="pelucia_abastecido_inicial"
                                            name="pelucia_abastecido_inicial"
                                            value="<%= estabelecimento.pelucia_abastecido_inicial || '' %>"
                                            placeholder="Ex.: 50"
                                        >
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="col-12">
                            <div class="route-coordinates-header route-coordinates-private">
                                <div>
                                    <label class="mb-1">Localização exata do ponto</label>
                                    <small class="modern-note">
                                        Se estiver no estabelecimento, clique no botão para atualizar a localização usada nas rotas.
                                    </small>

                                    <small
                                        class="coordinates-capture-status <%= estabelecimento.latitude && estabelecimento.longitude ? 'is-success' : '' %>"
                                        id="coordinatesCaptureStatus"
                                    >
                                        <%= estabelecimento.latitude && estabelecimento.longitude ? 'Localização já cadastrada para este ponto.' : 'Localização ainda não cadastrada.' %>
                                    </small>
                                </div>

                                <button type="button" class="btn-modern-secondary route-coordinate-button" id="captureCoordinatesButton">
                                    <i class="bi bi-crosshair"></i>
                                    <span>Atualizar com minha localização atual</span>
                                </button>
                            </div>

                            <input
                                type="hidden"
                                id="latitude"
                                name="latitude"
                                value="<%= estabelecimento.latitude ?? '' %>"
                            >

                            <input
                                type="hidden"
                                id="longitude"
                                name="longitude"
                                value="<%= estabelecimento.longitude ?? '' %>"
                            >
                        </div>
                    </div>
                </section>

                <section class="form-actions-bar estabelecimentos-form-actions">
                    <div class="form-actions-copy">
                        <strong>Atualização do cadastro</strong>
                        <span>Revise os dados do estabelecimento antes de confirmar as alterações.</span>
                    </div>

                    <div class="form-actions-buttons estabelecimentos-form-actions-buttons">
                        <a href="/estabelecimentos" class="btn-modern-secondary estabelecimento-action-button">
                            <i class="bi bi-arrow-left"></i>
                            <span>Cancelar</span>
                        </a>

                        <button type="submit" class="btn-modern estabelecimento-action-button">
                            <i class="bi bi-check2-circle"></i>
                            <span>Atualizar estabelecimento</span>
                        </button>
                    </div>
                </section>

            </form>
        </main>
    </div>

    <script src="/vendor/bootstrap/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const telefoneInput = document.getElementById('telefone_contato');
            const latitudeInput = document.getElementById('latitude');
            const longitudeInput = document.getElementById('longitude');
            const captureCoordinatesButton = document.getElementById('captureCoordinatesButton');
            const coordinatesCaptureStatus = document.getElementById('coordinatesCaptureStatus');
            const produtoInputs = Array.from(document.querySelectorAll('input[name="produto"]'));

            const bolinhasMachineFields = Array.from(document.querySelectorAll('[data-operational-field="bolinhas-machine"]'));
            const consignadosInitialFields = Array.from(document.querySelectorAll('[data-operational-field="consignados-initial"]'));
            const peluciasMachineFields = Array.from(document.querySelectorAll('[data-operational-field="pelucias-machine"]'));
            const peluciasInitialFields = Array.from(document.querySelectorAll('[data-operational-field="pelucias-initial"]'));

            const chaveBolinhasInput = document.getElementById('chave_bolinhas');
            const maquinaBolinhasInput = document.getElementById('maquina_bolinhas');
            const consignadoQuantidadeInicialInput = document.getElementById('consignado_quantidade_inicial');
            const chavePeluciasInput = document.getElementById('chave_pelucias');
            const maquinaPeluciasInput = document.getElementById('maquina_pelucias');
            const peluciaLeituraInicialInput = document.getElementById('pelucia_leitura_inicial');
            const peluciaAbastecidoInicialInput = document.getElementById('pelucia_abastecido_inicial');

            const initialNumberInputs = [
                consignadoQuantidadeInicialInput,
                peluciaLeituraInicialInput,
                peluciaAbastecidoInicialInput
            ];

            const validateInitialNumberInput = (input, label) => {
                if (!input) return null;

                const value = input.value.trim();

                if (!value) {
                    return `${label} é obrigatório.`;
                }

                if (!/^\d+$/.test(value)) {
                    return `${label} deve conter apenas números inteiros, sem letras, negativos, decimais ou notação científica.`;
                }

                const parsed = Number(value);

                if (!Number.isSafeInteger(parsed) || parsed < 0) {
                    return `${label} deve ser um número inteiro válido.`;
                }

                if (parsed > 100000) {
                    return `${label} não pode ser maior que 100000.`;
                }

                return null;
            };

            initialNumberInputs.forEach((input) => {
                input?.addEventListener('input', () => {
                    input.value = input.value.replace(/\D/g, '').slice(0, 6);
                });
            });

            const getSelectedProducts = () => produtoInputs
                .filter((input) => input.type === 'hidden' || input.checked)
                .map((input) => input.value);

            const setGroupVisibility = (fields, shouldShow) => {
                fields.forEach((field) => field.classList.toggle('d-none', !shouldShow));
            };

            const syncOperationalFields = () => {
                const selectedProducts = getSelectedProducts();

                const hasBolinhas = selectedProducts.includes('BOLINHAS');
                const hasConsignados = selectedProducts.includes('CONSIGNADOS');
                const hasPelucias = selectedProducts.includes('PELUCIAS');

                setGroupVisibility(bolinhasMachineFields, hasBolinhas);
                setGroupVisibility(consignadosInitialFields, hasConsignados);
                setGroupVisibility(peluciasMachineFields, hasPelucias);
                setGroupVisibility(peluciasInitialFields, hasPelucias);

                if (chaveBolinhasInput) chaveBolinhasInput.required = false;
                if (maquinaBolinhasInput) maquinaBolinhasInput.required = false;
                if (chavePeluciasInput) chavePeluciasInput.required = false;
                if (maquinaPeluciasInput) maquinaPeluciasInput.required = false;

                if (consignadoQuantidadeInicialInput) consignadoQuantidadeInicialInput.required = hasConsignados;
                if (peluciaLeituraInicialInput) peluciaLeituraInicialInput.required = hasPelucias;
                if (peluciaAbastecidoInicialInput) peluciaAbastecidoInicialInput.required = hasPelucias;

                if (!hasBolinhas) {
                    if (chaveBolinhasInput) chaveBolinhasInput.value = '';
                    if (maquinaBolinhasInput) maquinaBolinhasInput.value = '';
                }

                if (!hasConsignados && consignadoQuantidadeInicialInput) {
                    consignadoQuantidadeInicialInput.value = '';
                }

                if (!hasPelucias) {
                    if (chavePeluciasInput) chavePeluciasInput.value = '';
                    if (maquinaPeluciasInput) maquinaPeluciasInput.value = '';
                    if (peluciaLeituraInicialInput) peluciaLeituraInicialInput.value = '';
                    if (peluciaAbastecidoInicialInput) peluciaAbastecidoInicialInput.value = '';
                }
            };

            produtoInputs.forEach((input) => {
                if (input.type !== 'hidden') {
                    input.addEventListener('change', syncOperationalFields);
                }
            });

            syncOperationalFields();

            const applyTelefoneMask = () => {
                if (!telefoneInput) return;

                const digits = telefoneInput.value.replace(/\D/g, '').slice(0, 11);

                if (digits.length <= 2) {
                    telefoneInput.value = digits;
                    return;
                }

                if (digits.length <= 6) {
                    telefoneInput.value = `(${digits.slice(0, 2)}) ${digits.slice(2)}`;
                    return;
                }

                if (digits.length <= 10) {
                    telefoneInput.value = `(${digits.slice(0, 2)}) ${digits.slice(2, 6)}-${digits.slice(6)}`;
                    return;
                }

                telefoneInput.value = `(${digits.slice(0, 2)}) ${digits.slice(2, 7)}-${digits.slice(7)}`;
            };

            telefoneInput?.addEventListener('input', applyTelefoneMask);
            applyTelefoneMask();

            captureCoordinatesButton?.addEventListener('click', () => {
                if (!navigator.geolocation) {
                    window.AppAlerts?.warning('Seu navegador não permite capturar a localização atual.');
                    return;
                }

                captureCoordinatesButton.disabled = true;

                if (coordinatesCaptureStatus) {
                    coordinatesCaptureStatus.textContent = 'Capturando localização...';
                    coordinatesCaptureStatus.classList.remove('is-success');
                }

                navigator.geolocation.getCurrentPosition(
                    (position) => {
                        latitudeInput.value = position.coords.latitude.toFixed(6);
                        longitudeInput.value = position.coords.longitude.toFixed(6);

                        if (coordinatesCaptureStatus) {
                            coordinatesCaptureStatus.textContent = 'Localização atualizada com sucesso.';
                            coordinatesCaptureStatus.classList.add('is-success');
                        }

                        captureCoordinatesButton.disabled = false;
                        window.AppAlerts?.success('Localização do ponto atualizada com sucesso.');
                    },
                    () => {
                        if (coordinatesCaptureStatus) {
                            coordinatesCaptureStatus.textContent = 'Não foi possível atualizar a localização agora.';
                            coordinatesCaptureStatus.classList.remove('is-success');
                        }

                        captureCoordinatesButton.disabled = false;
                        window.AppAlerts?.error('Não foi possível ler sua localização atual.');
                    },
                    {
                        enableHighAccuracy: true,
                        timeout: 12000,
                        maximumAge: 60000
                    }
                );
            });

            document.getElementById('edicaoForm').addEventListener('submit', (e) => {
                const selectedProducts = getSelectedProducts();

                if (selectedProducts.length === 0) {
                    e.preventDefault();
                    window.AppAlerts?.warning('Nenhum produto ativo foi encontrado para esta edição.');
                    return;
                }

                if (selectedProducts.includes('CONSIGNADOS')) {
                    const consignadoError = validateInitialNumberInput(
                        consignadoQuantidadeInicialInput,
                        'Quantidade inicial deixada para consignados'
                    );

                    if (consignadoError) {
                        e.preventDefault();
                        window.AppAlerts?.warning(consignadoError);
                        consignadoQuantidadeInicialInput?.focus();
                        return;
                    }
                }

                if (selectedProducts.includes('PELUCIAS')) {
                    const leituraError = validateInitialNumberInput(
                        peluciaLeituraInicialInput,
                        'Leitura inicial de pelúcias'
                    );

                    if (leituraError) {
                        e.preventDefault();
                        window.AppAlerts?.warning(leituraError);
                        peluciaLeituraInicialInput?.focus();
                        return;
                    }

                    const abastecidoError = validateInitialNumberInput(
                        peluciaAbastecidoInicialInput,
                        'Quantidade inicial de pelúcias'
                    );

                    if (abastecidoError) {
                        e.preventDefault();
                        window.AppAlerts?.warning(abastecidoError);
                        peluciaAbastecidoInicialInput?.focus();
                        return;
                    }
                }
            });

            <% if (success) { %>
                window.AppAlerts?.success("<%- success.replace(/"/g, '&quot;') %>");
                setTimeout(() => {
                    window.location.href = '/estabelecimentos';
                }, 1800);
            <% } %>

            <% if (error) { %>
                window.AppAlerts?.error("<%- error.replace(/"/g, '&quot;') %>");
            <% } %>
        });
    </script>
</body>
</html>