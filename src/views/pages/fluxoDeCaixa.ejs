<!DOCTYPE html>
<html lang="pt-br">
<%- include('../partials/head', { title: 'Fluxo de Caixa' }) %>

<body class="list-page-body">
    <%- include('../partials/header') %>
    <div class="page-spacer"></div>

    <div class="list-shell">
        <%- include('../partials/breadcrumb', { pageTitle: 'Fluxo de Caixa' }) %>

        <section class="list-hero list-hero-neutral">
            <div class="list-hero-content">
                <span class="list-kicker"><i class="bi bi-graph-up-arrow"></i> Financeiro</span>
                <h1>Fluxo de caixa anual</h1>
                <p>Uma visão mais organizada do desempenho mensal, com resultados, entradas e saídas em uma tabela ampla, moderna e fácil de ler.</p>
            </div>
            <div class="list-hero-side">
                <div class="list-stat"><strong>Ano</strong><span><%= year %></span></div>
                <div class="list-stat"><strong>Visão</strong><span>Consolidado mensal do negócio</span></div>
            </div>
        </section>

        <main class="list-stage">
            <section class="list-panel">
                <div class="list-toolbar">
                    <div class="list-toolbar-copy">
                        <h2>Resumo financeiro de <%= year %></h2>
                        <p>Altere o ano para comparar rapidamente a evolução mensal de entradas, saídas e resultado acumulado.</p>
                    </div>
                    <div style="min-width:220px;">
                        <label for="anoInput" class="form-label">Ano para a pesquisa</label>
                        <input type="number" id="anoInput" class="form-control text-center" value="<%= year %>" onchange="atualizarAno()">
                    </div>
                </div>

                <div class="modern-table-wrap">
                    <div class="modern-table-scroll">
                        <table id="fluxoDeCaixaTable" class="modern-table">
                            <thead>
                                <tr>
                                    <th class="table-cell-finance">Resultado atual</th>
                                    <th class="table-cell-month">Janeiro</th>
                                    <th class="table-cell-month">Fevereiro</th>
                                    <th class="table-cell-month">Março</th>
                                    <th class="table-cell-month">Abril</th>
                                    <th class="table-cell-month">Maio</th>
                                    <th class="table-cell-month">Junho</th>
                                    <th class="table-cell-month">Julho</th>
                                    <th class="table-cell-month">Agosto</th>
                                    <th class="table-cell-month">Setembro</th>
                                    <th class="table-cell-month">Outubro</th>
                                    <th class="table-cell-month">Novembro</th>
                                    <th class="table-cell-month">Dezembro</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr class="table-row-balance-highlight">
                                    <%
                                    const currentMonth = new Date().getMonth();
                                    let somaTotalAnualCorrente = 0;
                                    for (let i = 0; i <= currentMonth; i++) {
                                        somaTotalAnualCorrente += somaMensal[i] || 0;
                                    }
                                    %>
                                    <td class="table-cell-total">
                                        <span class="table-badge <%= somaTotalAnualCorrente >= 0 ? 'table-badge-green' : 'table-badge-danger' %>">
                                            <%= somaTotalAnualCorrente.toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' }) %>
                                        </span>
                                    </td>
                                    <% somaMensal.forEach(function(total) { %>
                                        <td class="table-cell-total">
                                            <strong style="color:<%= total > 0 ? '#15803d' : (total < 0 ? '#b91c1c' : '#0f172a') %>;">
                                                <%= total.toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' }) %>
                                            </strong>
                                        </td>
                                    <% }) %>
                                </tr>

                                <tr class="table-row-entry-highlight">
                                    <td class="table-title-cell table-cell-total">Total entradas</td>
                                    <% totaisEntradas.forEach(function(total) { %>
                                        <td class="table-cell-total"><strong style="color:#15803d;"><%= total.toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' }) %></strong></td>
                                    <% }) %>
                                </tr>
                                <% for (let tipo in dadosEntradas) { %>
                                    <tr>
                                        <td class="table-muted table-cell-wrap"><%= tipo %></td>
                                        <% for (let i = 0; i < 12; i++) { %>
                                            <td style="color:#16a34a;"><%= dadosEntradas[tipo][i].toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' }) %></td>
                                        <% } %>
                                    </tr>
                                <% } %>

                                <tr class="table-row-exit-highlight">
                                    <td class="table-title-cell table-cell-total">Total saídas</td>
                                    <% totaisSaidas.forEach(function(total) { %>
                                        <td class="table-cell-total"><strong style="color:#dc2626;"><%= total.toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' }) %></strong></td>
                                    <% }) %>
                                </tr>
                                <% for (let tipo in dadosSaidas) { %>
                                    <tr>
                                        <td class="table-muted table-cell-wrap"><%= tipo %></td>
                                        <% for (let i = 0; i < 12; i++) { %>
                                            <td style="color:#dc2626;"><%= dadosSaidas[tipo][i].toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' }) %></td>
                                        <% } %>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </section>
        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
    <script>
    function atualizarAno() {
        const ano = document.getElementById('anoInput').value;
        window.location.href = `/fluxo-de-caixa?year=${ano}`;
    }

    document.addEventListener('DOMContentLoaded', () => {
        document.getElementById('anoInput').addEventListener('change', atualizarAno);
    });
    </script>
</body>
</html>
